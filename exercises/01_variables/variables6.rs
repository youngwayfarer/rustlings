// TODO: Change the line below to fix the compiler error.
const NUMBER: i32 = 3;
// const must be typed, so we need to specify the type of the variable

fn main() {
    println!("Number: {NUMBER}");
}
